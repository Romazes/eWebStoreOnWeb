@model UserClaimsViewModel

@{
    ViewData["Title"] = "Edit Claims for the User";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@{
    var userId = ViewBag.userId;
    var userName = ViewBag.userName;
}

<form method="post">
    <div class="card">
        <div class="card-header">
            <h2>
                Add or remove claims for user: @userName
            </h2>
        </div>
        <div class="card-body">
            @for (int i = 0; i < Model.Cliams.Count; i++)
            {
                <div class="form-check m-1">
                    <input type="hidden" asp-for="@Model.Cliams[i].ClaimType" />
                    <input asp-for="@Model.Cliams[i].IsSelected" class="form-check-input" />
                    <label class="form-check-label" asp-for="@Model.Cliams[i].IsSelected">
                        @Model.Cliams[i].ClaimType
                    </label>
                </div>
            }
            <div asp-validation-summary="All" class="text-danger"></div>
        </div>

        <div class="card-footer">
            <a class="btn btn-secondary text-white" asp-action="EditUser" asp-route-id="@userId" accesskey="c">
                <i class="fas fa-undo"></i> <u>C</u>ancel
            </a>

            <button type="submit" class="btn btn-danger text-white" accesskey="u">
                <i class="fas fa-edit"></i> <u>U</u>pdate
            </button>
        </div>

    </div>
</form>