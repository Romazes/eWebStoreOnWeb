@model HomeViewModel

@await Html.PartialAsync("_Carousel")

<h2>Prefered tea of the week</h2>
<h4>Clients' preferences - just for you!</h4>

<div class="row">
    @foreach (var product in Model.ProductsOfTheWeek)
    {
        @await Html.PartialAsync("_ProductCard", product)
    }
</div>